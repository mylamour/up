---
name: example
description: Example slash command for the plugin
allowed-tools:
  - Read
  - Bash(git *)
---

# /example — Example Command

This is an example slash command. Replace this with your plugin's functionality.

1. Describe what the command does step by step.
2. The AI will follow these instructions when the user invokes the command.
